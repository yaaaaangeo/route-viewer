# Route Viewer 실행 방법

## 1. 데스크톱 앱 (권장)

`release/Route Viewer Setup 3.0.0.exe` 를 실행해서 설치하면
바탕화면 아이콘과 시작 메뉴에 등록됩니다. 더블클릭으로 실행하세요.

주행 기록은 아래 SQLite 파일에 **영구 저장**됩니다.

```
%APPDATA%\Route Viewer\database\route-viewer.db
```

앱을 껐다 켜도, PC를 재부팅해도 그대로 남아 있습니다.
예전 엑셀 파일을 다시 넣을 필요가 없습니다.

설치 없이 쓰려면 `release/RouteViewer-portable-3.0.0.exe` 를 그냥 실행하면 됩니다.

---

## 2. 브라우저로 열기 (선택)

```powershell
node server.js
```

브라우저에서 `http://localhost:8080` 으로 접속합니다.
같은 네트워크의 다른 기기에서는 이 PC의 IP 주소로 접속하면 됩니다.

```text
http://PC_IP_ADDRESS:8080
```

이때 데이터는 **접속한 브라우저의 IndexedDB**에 저장됩니다.
브라우저마다 따로 저장되므로, 여러 사람이 같은 기록을 봐야 한다면
아래 "여러 사람이 같은 기록 보기"를 참고하세요.

> v2까지는 `localStorage`에 전체 GPS 데이터를 JSON 한 덩어리로 넣었습니다.
> 용량 한계(보통 5MB)에 금방 걸려서 v3부터는 IndexedDB를 씁니다.
> 예전 `localStorage` 데이터가 남아 있으면 처음 열 때 자동으로 옮겨옵니다.

---

## 여러 사람이 같은 기록 보기 — 서버 동기화 (권장)

한 사람이 서버를 띄웁니다.

```powershell
node server.js
```

그 다음, 모두가 데스크톱 앱의 **[데이터 관리] 탭**에서 "여러 명이 같은 기록
보기(서버 동기화)" 패널에 그 PC의 주소를 넣고 **"지금 동기화"**를 누릅니다.

```
서버 주소: http://192.168.0.5:8080
```

동기화를 누르면 다음 두 단계가 일어납니다.

1. 서버에 있는 기록을 받아서 내 SQLite에 **병합**(merge)
2. 방금 병합된 내 기록 전체를 서버로 올려서 서버도 **병합**

둘 다 "추가"만 하므로(교체 아님) 누가 언제 눌러도, 순서가 엇갈려도
기록이 지워지거나 두 배로 부풀지 않습니다. 중복 판정은 데스크톱 SQLite와
똑같은 규칙(`date|time|vehicle|lat|lng`)을 씁니다.

"앱을 켤 때 자동으로 동기화"를 켜두면 앱을 실행할 때마다 조용히 한 번
맞춰봅니다(서버가 꺼져 있으면 그냥 넘어가고, 알림도 띄우지 않습니다).

같은 서버 주소는 **앱 자동 업데이트 배포 주소**로도 함께 쓰입니다
(`<주소>/updates`). 아래 "새 버전 배포하기"를 참고하세요.

서버가 없어도 되는 방법은 **백업 파일을 주고받는 것**입니다.

```
A: [백업 저장] → route-viewer-backup_2026-08-26.json
B: [백업 복구] → "병합 복구" 선택
```

## 저장 암호 (선택)

공개 주소로 배포한다면 아무나 기록을 덮어쓰지 못하게 저장 암호를 걸 수 있습니다.

```powershell
$env:ROUTE_VIEWER_WRITE_TOKEN="원하는암호"
node server.js
```

데스크톱 앱의 동기화 패널 "저장 암호" 칸에 같은 값을 넣으면 됩니다.
읽기(달력 보기)는 암호 없이도 되고, 쓰기(동기화로 올리기)에만 암호를 확인합니다.

## 새 버전 배포하기 (자동 업데이트)

```powershell
# 1) package.json 의 version 을 올린다 (예: 3.0.0 → 3.0.1)
npm run dist          # release/ 에 새 설치 파일 + latest.yml 생성
node server.js         # 이 PC가 서버 역할을 한다
```

`release/` 폴더는 server.js가 `/updates` 로 그대로 내려주므로, 이미 설치된
앱들은 앱을 켤 때(또는 [도움말] → "업데이트 확인") 자동으로 새 버전을
찾아서 물어보고, 받은 뒤 다시 시작하면 설치됩니다.

앱이 **설치되지 않고 개발 모드로 실행 중**(`npm start`)이면 업데이트
확인 자체를 하지 않습니다. 실제 설치된 `.exe`에서만 동작합니다.
